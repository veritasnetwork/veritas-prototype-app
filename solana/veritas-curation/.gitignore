.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Keypairs (NEVER commit these!)
keys/*.json
!keys/.gitkeep

# Deployment artifacts
.deploy/
deployments/*.json
!deployments/.gitkeep

# Local scripts (keep out of git)
scripts/update-min-deposit.ts
